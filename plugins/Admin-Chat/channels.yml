channels:
  adminchat:
    command: 'ac'
    format: '&c[ADMIN CHAT]:&r {NAME}: &c{MESSAGE}'
  main:
    formatted-name: true
    command: 'main'
    format: '{PREFIX}{DISPLAYNAME}&f: {SUFFIX}{MESSAGE}'
